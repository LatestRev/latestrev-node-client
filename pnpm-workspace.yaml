minimumReleaseAge: 10080 # 7 days — mirror the Azure Artifacts feed's minimum age
minimumReleaseAgeExclude:
    - '@thinkpixellab/*'

allowBuilds:
    esbuild: true # needs its postinstall to fetch the platform binary
